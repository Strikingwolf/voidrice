#!/bin/fish
alias ka="killall"
alias mkd="mkdir -pv"
alias mpv="mpv --input-ipc-server=/tmp/mpvsoc$(date +%s)"
alias calcurse="calcurse -D ~/.config/calcurse"
alias magit="nvim -c MagitOnly"
alias p="sudo pacman"
alias SS="sudo systemctl"
alias f="$FILE"
alias g="git"
alias trem="transmission-remote"
alias e="$EDITOR"
alias v="$EDITOR"
alias x="sxiv -ft *"
alias sdn="sudo shutdown -h now"
alias gua="git remote | xargs -L1 git push --all"
alias ls="ls -hN --color=auto --group-directories-first"
alias grep="grep --color=auto"
alias diff="diff --color=auto"
alias ccat="highlight --out-format=ansi"
alias yt="youtube-dl --add-metadata -i -o '%(upload_date)s-%(title)s.%(ext)s'"
alias yta="yt -x -f bestaudio/best"
alias YT="youtube-viewer"
alias ref="shortcuts >/dev/null; source ~/.config/shortcutrc"
alias upc="sudo pacman -Syu; pkill -RTMIN+8 i3blocks"
alias lsp="pacman -Qett --color=always | less"
alias ffmpeg="ffmpeg -hide_banner"
alias cpng="xclip -selection clipboard -t image/png"
alias java8="/usr/lib/jvm/java-8-openjdk/jre/bin/java"
alias admintask="pandoc vimwiki/Admin\ Tasks.md -o /tmp/admin.pdf && zathura /tmp/admin.pdf"
alias setlocaltime="curl https://ipapi.co/timezone | xargs timedatectl set-timezone"
alias youboat="newsboat -C $HOME/.config/newsboat/youtube"
command -v nvim >/dev/null && alias vim="nvim" && alias vimdiff="nvim -d" # Use neovim for vim if present

vf() { fzf | xargs -r -I % $EDITOR % ;}
