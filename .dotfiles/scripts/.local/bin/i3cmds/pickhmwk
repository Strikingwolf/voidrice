#!/bin/bash

file=$(find $1 -type f | grep -E ".*\.(djvu|pdf|tex|md)\$" | sort | dmenu -i -l 10 | sed 's| |\\\ |g')

echo $file

[ -z $file ] && exit
ext="${file##*.}"

if [ "$ext" == "djvu" ] || [ "$ext" == "pdf" ]; then
	zathura "$file"
else
	$TERMINAL -e $EDITOR $file
fi
